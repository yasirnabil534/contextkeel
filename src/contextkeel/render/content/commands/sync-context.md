---
name: sync-context
description: Sync the vault context store with recent changes
---
Use the `update-context` skill: regenerate the the code index code graph and update
`Vault/Context/` notes (Tech Stack, Architecture rationale, API Contracts,
Glossary) to reflect recent changes, add an ADR for any significant decision,
and prepend a plain-language `Vault/Changelog.md` entry if a feature/module
just shipped.
