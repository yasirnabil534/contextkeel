---
name: test
description: Run the project's test suite (stack-aware)
---
Use the `run-tests` skill: detect the stack's test command from `project.yml` and
manifests, run the suite, and fix failures at the root cause until green.
