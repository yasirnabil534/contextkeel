---
name: context
description: Load project context before working
---
Use the `load-context` skill: read `project.yml` and `Vault/Context/`, resolve the
stack, and summarize the relevant modules before doing any work.
