---
name: commit
description: Propose a commit message for staged changes
---
Run the `write-commit` skill: analyze staged changes and propose a commit message
in the project's style (Conventional Commits by default). Only run `git commit` if
I explicitly ask.
