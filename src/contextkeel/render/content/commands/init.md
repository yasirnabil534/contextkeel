---
name: init
description: Bootstrap an empty repo into a runnable project from project.yml
---
Run the `bootstrap-project` skill: turn `project.yml` (resolving auto/defaults)
into a runnable skeleton — stack init, directory layout, tooling, dependencies,
and the initial `Vault/Context/` notes. Confirm the resolved stack before
generating files.
